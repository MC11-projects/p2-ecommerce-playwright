{
  "name": "deals-vouchers-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:ui": "playwright test --grep @ui",
    "test:api": "playwright test --grep @api",
    "test:auth": "playwright test --grep @auth",
    "test:deals": "playwright test --grep @deals",
    "test:cart": "playwright test --grep @cart",
    "test:checkout": "playwright test --grep @checkout",
    "test:vouchers": "playwright test --grep @vouchers",
    "test:slow": "playwright test --grep @slow",
    "test:filters": "playwright test --grep @filters"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "allure-playwright": "^3.6.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
